import styled from 'styled-components';

const Wrapper = styled.div``;

function ListGroup() {
  return (
    <Wrapper>
      {/* Title
      Mapped list items */}
    </Wrapper>
  );
}

export default ListGroup;
